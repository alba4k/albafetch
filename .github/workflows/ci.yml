name: ci

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        include:
          - os: ubuntu-latest
            platform: linux
          - os: macos-latest
            platform: darwin

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v3

      - uses: cachix/install-nix-action@v22

      - uses: cachix/cachix-action@v12
        with:
          name: albafetch
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"

      - name: build (x64)
        run: nix build --accept-flake-config .#crossPackages.static.${{ matrix.platform }}.albafetch

      - name: test
        run: ./result/bin/albafetch

      - uses: actions/upload-artifact@v3
        if: runner.os == 'Linux'
        with:
          name: albafetch-linux-x64-static
          path: ${{ github.workspace }}/result/bin/albafetch

      - uses: actions/upload-artifact@v3
        if: runner.os == 'macOS'
        with:
          name: albafetch-darwin-x64-static
          path: ${{ github.workspace }}/result/bin/albafetch

      - name: build (linux/aarch64-multiplatform)
        if: runner.os == 'Linux'
        run: nix build --accept-flake-config .#crossPackages.arm.linux.albafetch

      - uses: actions/upload-artifact@v3
        if: runner.os == 'Linux'
        with:
          name: albafetch-linux-aarch64-static
          path: ${{ github.workspace }}/result/bin/albafetch
